{% extends 'base.html.twig' %}

{% block title %}Édition{% endblock %}

{% block body %}
    <h1 class="h3">Édition</h1>

    {{ include('roles/_form.html.twig', {'button_label': 'Valider'}) }}

    <a href="{{ path('roles_index') }}" class="btn btn-secondary btn-icon-split float-right">
        <span class="icon text-gray-600">
            <i class="fas fa-arrow-left"></i>
        </span>
        <span class="text">Retour</span>
    </a>

    {{ include('roles/_delete_form.html.twig') }}
{% endblock %}
