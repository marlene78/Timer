{% extends 'base.html.twig' %}

{% block title %}Édition{% endblock %}

{% block body %}
    <h1>Modifier mes informations</h1>

    {{ include('user/_form_edit.html.twig', {'button_label': 'Mettre à jour'} ) }}

    <br><br>

    <a href="{{ path('home') }}" class="btn btn-secondary btn-icon-split float-right">
        <span class="icon text-gray-600">
            <i class="fas fa-arrow-left"></i>
        </span>
        <span class="text">Retour</span>
    </a>

    <a href="{{ path('change_password' , {'id': app.user.id }) }}" class="btn btn-warning btn-icon-split">
        <span class="icon text-white-50"><i class="fas fa-exclamation-triangle"></i></span>
        <span class="text">Modifier mon mot de passe</span> 
    </a>

  

 
{% endblock %}
