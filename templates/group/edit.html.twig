{% extends 'base.html.twig' %}

{% block title %}Édition{% endblock %}

{% block body %}
    <h1>Édition</h1>

    {{ include('group/_form.html.twig', {'button_label': 'Mettre à jour'}) }}


    <a href="{{ path('group_index') }}" class="btn btn-secondary btn-icon-split float-right">
        <span class="icon text-gray-600">
            <i class="fas fa-arrow-left"></i>
        </span>
        <span class="text">Retour</span>
    </a>
  
    <br>

   {{ include('group/_delete_form.html.twig') }}
{% endblock %}
