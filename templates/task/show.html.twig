{% extends 'base.html.twig' %}

{% block title %}Task{% endblock %}

{% block body %}

    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">{{ task.nom }}</h6>
        </div>
        <div class="card-body">
            <table class="table">
                <tbody>
                    <tr>
                        <th>Nom</th>
                        <td>{{ task.nom }}</td>
                    </tr>
                    <tr>
                        <th>Date de debut</th>
                        <td>{{ task.dateDeDebut ? task.dateDeDebut|date('d-m-Y') : '' }}</td>
                    </tr>
                    <tr>
                        <th>Date de Fin</th>
                        <td>{{ task.dateDeFin ? task.dateDeFin|date('d-m-Y') : '' }}</td>
                    </tr>
                    {% if task.projet.createur == app.user %}
                    <tr>
                        <td colspan="2">
                            <a href="{{ path('task_edit', {'id': task.id}) }}" class="btn btn-info btn-icon-split">
                                <span class="icon text-white-50">
                                    <i class="fas fa-edit"></i>
                                </span>
                                <span class="text">Mettre à jour</span>
                            </a>
                        </td>
                    <tr>
                    {% endif %}
                </tbody>
            </table>
        </div>
    </div>

    <a href="{{ path('project_show' , {'id' : task.projet.id }) }}" class="btn btn-secondary btn-icon-split float-right">
        <span class="icon text-gray-600">
            <i class="fas fa-arrow-left"></i>
        </span>
        <span class="text">Voir le projet</span>
    </a>




    {{ include('task/_delete_form.html.twig') }}
{% endblock %}
